%div.row
  %div.col-sm-12
    %div.page-title-box
      %h4.page-title Thông báo
      %ol.breadcrumb.float-right
        %li.breadcrumb-item.active
          %a{href: '/'}
      %div.clearfix
    .box-body
      .form-group.area-add
        = link_to 'Add New', new_super_dashboard_message_path, class: 'btn btn-primary'
    .box-body
      %table.table.table-bordered.table-responsive
        %thead
          %tr
            %th.text-center{style: 'width: 10%'}
              ID
            %th.text-center{style: 'width: 70%'}
              Tiêu đề
            %th{style: 'width: 10%'}
            %th{style: 'width: 10%'}
        %tbody
          - @dashboard_messages.each do |dashboard_message|
            %tr
              %td.text-center= dashboard_message.id
              %td= dashboard_message.title
              %td.text-center= link_to '', edit_super_dashboard_message_path(dashboard_message), class: 'fa fa-edit'
              %td.text-center
                - if can? :delete, dashboard_message
                  = link_to '', super_dashboard_message_path(dashboard_message), class: 'fa fa-times', method: :delete, data: { confirm: 'Are you sure?' }
    .box-footer.clearfix
      %ul.pagination.pagination-sm.no-margin.pull-right
        = paginate @dashboard_messages
