%table.table.table-bordered
  %thead
    %tr
      %th{style: 'width: 20%;'}
        = sort_link(@q, :id, {} , {remote: true})
      %th{style: 'width: 50%;'}
        = sort_link(@q, :name, {}, {remote: true})
      %th{style: 'width: 10%;'} edit
      %th{style: 'width: 10%;'} delete
      %th{style: 'width: 10%;'} copy

  %tbody
    - @product_types.each do |product_type|
      %tr
        %td= product_type.id
        %td= product_type.name
        %td= link_to '', edit_product_type_path(product_type), class: 'fa fa-edit'
        %td
          - if can? :delete, product_type
            = link_to '', product_type_path(product_type), class: 'fa fa-times',
             method: :delete, data: { confirm: 'Are you sure?' }, remote: true
        %td
          = link_to '', copy_product_type_path(product_type), class: 'fa fa-copy'
